# CC Kickoff Prompt: CCC v1.0.7 — Stage 01c

## Read First
- Concept doc: `docs/v1.0/v1.0.7/CCC_concept_v1.0.7.md`
- Tasklist: `docs/v1.0/v1.0.7/CCC_tasklist_v1.0.7.md`
- Depends on: Stage 01b GO
- Source file: `CLAUDE.md` (project root)

## Scope
Documentation update only. No source code changes. Covers: `CLAUDE.md` regex update, Roadmap, Changelog, and push to GitHub + Forgejo.

---

## Task 1 — Update CLAUDE.md

Read `CLAUDE.md` at the project root.

In the **Stage Gate Process** section, find:

```
MUST use `_test_stage` — CCC treeview regex: `/_test_stage\d+\.md$/`
```

Replace with:

```
MUST use `_test_stage` — CCC treeview regex: `/_test_stage\d+[a-z]?\d*\.md$/`
Supports main stages (e.g. `CCC_test_stage11.md`), sub-stages (`CCC_test_stage11a.md`), and fixes (`CCC_test_stage11a01.md`).
```

---

## Task 2 — Update Roadmap

Open `docs/CCC_Roadmap.md`. Under v1.0.7, mark Stage 01 (sub-stages 01a, 01b, 01c) as complete.

---

## Task 3 — Update Changelog

Open `CHANGELOG.md`. Add a v1.0.7 entry with the following:
- 01a: Regex fixes + JSDoc in `src/versions.js`
- 01b: API endpoint + `getTestFilePath()` fix (string stage IDs, HTTP 400 on invalid input)
- 01c: `CLAUDE.md` regex updated to support sub-stage and fix test file naming

---

## Task 4 — Push to GitHub and Forgejo

```
git add .
git commit -m "v1.0.7 Stage 01 complete — versions.js regex/JSDoc, API fixes, CLAUDE.md update"
git tag v1.0.7
git push origin main --tags
git push forgejo main --tags
```

---

## Task 5 — Generate test file

Generate `docs/v1.0/v1.0.7/CCC_test_stage01c.md`.

Test items must be checkboxes with observable outcomes. Cover:
- `CLAUDE.md` Stage Gate Process section contains updated regex `/_test_stage\d+[a-z]?\d*\.md$/`
- `CLAUDE.md` mentions sub-stages and fixes as supported naming patterns
- Old regex `/_test_stage\d+\.md$/` does not appear anywhere in `CLAUDE.md`
- `docs/CCC_Roadmap.md` shows v1.0.7 Stage 01 (01a, 01b, 01c) as complete
- `CHANGELOG.md` contains a v1.0.7 entry covering all three sub-stages
- `git log` shows commit tagged `v1.0.7`
- `git remote show origin` and `git remote show forgejo` confirm the tag was pushed to both remotes
- No source code files were changed in this sub-stage

---

## Rules
- Change only the one regex reference in `CLAUDE.md`
- Do NOT change any source code files
- Do NOT restructure or reformat other sections of `CLAUDE.md`
